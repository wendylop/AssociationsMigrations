<%= form_for(professional_recipe) do |f| %>
  <% if professional_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professional_recipe.errors.count, "error") %> prohibited this professional_recipe from being saved:</h2>

      <ul>
      <% professional_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :reviews %>
    <%= f.number_field :reviews %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
