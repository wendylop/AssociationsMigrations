<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @recipe.name %>
</p>

<p><strong>Categories:</strong>
  <ol>
  <% @recipe_category.each do |ba| %>
     <li><%= ba.recipe.name %></li>
  <% end %>
  </ol>
</p>
<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
